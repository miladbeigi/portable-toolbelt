name: Tests
permissions:
  contents: read

on:
  push:
    branches: main
  pull_request:
    branches: main

jobs:
  integration:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [ubuntu, alpine, redhat]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and test ${{ matrix.os }} integration
        run: |
          echo "🔧 Building ${{ matrix.os }} integration test container..."
          docker build --build-arg PROFILE=all -f tests/integration/${{ matrix.os }}.Dockerfile -t toolbelt-${{ matrix.os }}-test .